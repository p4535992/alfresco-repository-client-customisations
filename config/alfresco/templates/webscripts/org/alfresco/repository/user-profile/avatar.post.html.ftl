<#-- Yes,this is really JSON.  But it needs to be called like it's HTML in order to load into an iFrame in Firefox 3.x-->
{
 "result": "${result}",
 "message": "${message}",
 "nodeRef": "${nodeRef.nodeRef}"
}